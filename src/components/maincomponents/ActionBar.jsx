import React from 'react';
import '../../css/ActionBarContainer.css';  

function ActionBar(props) {
    return (
        <> 
            {props.children}
        </>
    );

    
}

export default ActionBar;
